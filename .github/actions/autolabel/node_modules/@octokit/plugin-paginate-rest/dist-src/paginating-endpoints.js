import { paginatingEndpoints, } from "@octokit/plugin-paginate-rest/dist-src/generated/paginating-endpoints";
export { paginatingEndpoints } from "@octokit/plugin-paginate-rest/dist-src/generated/paginating-endpoints";
export function isPaginatingEndpoint(arg) {
    if (typeof arg === "string") {
        return paginatingEndpoints.includes(arg);
    }
    else {
        return false;
    }
}
